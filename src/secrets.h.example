/**
 * @file secrets.h.example
 * @brief Example secrets configuration file
 * 
 * Copy this file to secrets.h and fill in your WiFi credentials.
 * secrets.h is ignored by git for security.
 * 
 * Usage:
 *   cp src/secrets.h.example src/secrets.h
 *   # Edit secrets.h with your WiFi credentials
 *   pio run --target upload
 */

#ifndef SECRETS_H
#define SECRETS_H

// WiFi credentials - fill in your network details
#define WIFI_SSID ""
#define WIFI_PASSWORD ""

// Optional: Static IP configuration (leave empty to use DHCP)
// Format: "192.168.1.100"
#define STATIC_IP ""
#define GATEWAY_IP ""
#define SUBNET_MASK ""
#define DNS_IP ""

#endif // SECRETS_H
